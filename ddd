import time

start = time.time()  # zapisz czas początkowy

# kod, którego czas wykonania chcesz zmierzyć
for i in range(1000000):
    pass

end = time.time()  # zapisz czas końcowy
elapsed = end - start

print(f"Czas wykonania: {elapsed:.4f} sekund")
