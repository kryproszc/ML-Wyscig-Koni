@njit
def zastap_zera_nan(tablica):
    for i in range(tablica.shape[0]):
        for j in range(tablica.shape[1]):
            if tablica[i, j] == 0:
                tablica[i, j] = np.nan
    return tablica


a = zastap_zera_nan(a)
