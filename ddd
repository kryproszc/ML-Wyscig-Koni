import numpy as np
from numba import njit

@njit
def spolczynnik_reg_factor_P_to_I(a, b, k_start, k_stop):
    k_values = np.arange(k_start, k_stop + 1)
    exponent = np.exp(a * k_values + b)
    wartosci_reg = 1.0 - exponent
    return wartosci_reg
