dane <- read.csv("ścieżka/do/pliku.csv", stringsAsFactors = FALSE)

# Usunięcie spacji z początku i końca kolumny X
dane$X <- trimws(dane$X)

# Filtrowanie wierszy, gdzie kolumna X to "ZĄBKI"
zabki_wiersze <- dane[dane$X == "ZĄBKI", ]

# Wyświetlenie wyników
print(zabki_wiersze)
